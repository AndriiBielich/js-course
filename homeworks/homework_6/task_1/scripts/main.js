const calculateStringLength = (str) => console.log(`String: "${str}" has length - ${str.length}`);
const strings = ['hello world', 'lorem ipsum', 'javascript is cool'];
strings.forEach((string) => calculateStringLength(string));
